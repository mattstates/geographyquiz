import React from 'react';

/**
 * TODO: Address these issues...
 * Needs to have state to manage login button, or greeting to the user.
 * Other navigation?
 * Social Media Sharing?
 */

export default function Header() {
    return (
        <header>
            <a>Login</a>
        </header>
    );
}
